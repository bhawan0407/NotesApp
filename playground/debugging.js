var person = 
{
    name : 'Bhawan'
};

person.age = 25;

debugger; // Put this statement to stop the debugger at this point
// Use node inspect file-name to run the file in debug mode.
// In debug mode, type 'repl' and then press enter to enter the console window like interface.
// where we can see the value of the different variables in the current state.

person.name = 'Bhawandeep';

console.log(person);

